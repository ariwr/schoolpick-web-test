# Python 의존성 패키지 목록

# FastAPI 및 관련 패키지
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic>=2.10.0
pydantic-settings>=2.7.0

# 데이터베이스
sqlalchemy>=2.0.36
alembic>=1.14.0
# PostgreSQL 드라이버 (Windows에서는 psycopg2-binary 권장)
psycopg2-binary==2.9.11  # Windows 친화적, 설치 쉬움
# 또는 psycopg3 사용시:
# psycopg==3.1.18

# 인증 및 보안
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# 유틸리티
python-dotenv==1.0.0
httpx==0.27.2  # OpenAI 1.12.0과 호환되도록 다운그레이드 (0.28.0+ 에서 proxies 파라미터 제거됨)
email-validator==2.1.0
boto3>=1.28.0  # AWS SDK (Secrets Manager 사용)

# 개발 및 테스트
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.11.0
flake8==6.1.0
mypy==1.7.1

# 로깅
loguru==0.7.2

# CORS
fastapi-cors==0.0.6

# OpenAI API
openai>=1.55.0

# QR 코드 기반 출석 시스템
PyJWT==2.8.0  # JWT 토큰 생성 및 검증
qrcode  # QR 코드 생성 (Pillow 포함)
Pillow  # 이미지 처리 (Python 3.14 호환을 위해 버전 고정 제거)

# 배치 작업 스케줄러 (야자 출석 자동 처리용)
APScheduler==3.10.4

# 형태소 분석기 (세특 점검 규칙 기반 필터용)
kiwipiepy>=0.19.0

# OCR 및 PDF 처리
pytesseract>=0.3.10
pdf2image>=1.16.3
opencv-python-headless>=4.8.0  # 이미지 전처리용 (headless 버전 사용으로 numpy 충돌 방지)
numpy>=1.24.0  # 이미지 처리용
