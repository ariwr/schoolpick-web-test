{
  "name": "backend-teacher",
  "version": "1.0.0",
  "description": "스쿨픽 교사용 백엔드 서버",
  "main": "main.py",
  "scripts": {
    "dev": "python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "dev:setup": "powershell -ExecutionPolicy Bypass -File ./start_server.ps1",
    "start": "python -m uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "build": "echo 'No build step required for Python'",
    "test": "pytest",
    "lint": "flake8 app/",
    "format": "black app/",
    "db:migrate": "alembic upgrade head",
    "db:revision": "alembic revision --autogenerate -m"
  },
  "dependencies": {},
  "devDependencies": {},
  "keywords": ["schoolpick", "teacher", "backend", "fastapi"],
  "author": "SchoolPick Team",
  "license": "MIT"
}
